{
  "build": {
    "command": "cd photomagic/frontend && rm -rf node_modules package-lock.json && npm install --no-audit --no-fund --loglevel=error && npx vite build",
    "output_dir": "photomagic/frontend/dist"
  },
  "environment_variables": {
    "NODE_VERSION": "18"
  }
}