{
  "build": {
    "command": "cd frontend && npm install --no-audit --no-fund --loglevel=error && npx vite build",
    "output_dir": "frontend/dist"
  },
  "environment_variables": {
    "NODE_VERSION": "18"
  }
}